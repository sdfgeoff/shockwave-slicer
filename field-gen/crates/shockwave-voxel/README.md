# shockwave-voxel

Voxel occupancy and field propagation.

This crate owns voxelizing triangle geometry and propagating scalar fields through occupied voxel grids. It should remain focused on voxel-space algorithms rather than isosurface meshing, toolpath planning, or printer-specific G-code.
