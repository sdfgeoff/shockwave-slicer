# shockwave-path

Toolpath extraction from layer meshes.

This crate owns converting clipped layer surfaces into printable path primitives such as perimeters and infill. It should produce toolpaths, not firmware-specific G-code or end-to-end slicing jobs.
