# shockwave-mesh

Shared triangle mesh representation.

This crate owns the common mesh data type used between geometry, isosurface, clipping, and pathing crates. It should stay as a neutral data model rather than accumulating algorithms or file format policy.
